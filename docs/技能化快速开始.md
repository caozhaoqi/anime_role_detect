# Anime Role Detect - 技能化快速开始指南

---

## 1. 项目结构初始化

### 1.1 创建目录结构

```bash
cd /Users/caozhaoqi/PycharmProjects/anime_role_detect

# 创建技能目录
mkdir -p skills/ardc-client/scripts
mkdir -p skills/ardc-client/references
mkdir -p skills/ardc-collector/scripts
mkdir -p skills/ardc-cleaner/scripts
mkdir -p skills/ardc-trainer/scripts
mkdir -p skills/ardc-classifier/scripts
mkdir -p skills/ardc-search/scripts
mkdir -p skills/ardc-analyzer/scripts

# 创建配置目录
mkdir -p config

# 创建文档目录
mkdir -p docs/skills
```

### 1.2 创建环境配置文件

```bash
# 创建默认环境配置
cat > config/.env <<EOF
# Anime Role Detect 环境配置
ARDC_BASE_URL=http://localhost:8080
ARDC_API_KEY=your_api_key_here
ARDC_TOKEN=your_token_here

# 可选：多环境支持
# ARDC_ENV=dev  # dev/prod
EOF

# 创建开发环境配置
cat > config/.env.dev <<EOF
ARDC_BASE_URL=http://localhost:8080
ARDC_API_KEY=dev_api_key
ARDC_TOKEN=dev_token
ARDC_ENV=dev
EOF

# 创建生产环境配置
cat > config/.env.prod <<EOF
ARDC_BASE_URL=https://api.anime-role-detect.io
ARDC_API_KEY=prod_api_key
ARDC_TOKEN=prod_token
ARDC_ENV=prod
EOF
```

---

## 2. 核心客户端实现

### 2.1 创建 ardc-client 核心模块

**文件**: `skills/ardc-client/scripts/ard_client.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Anime Role Detect 核心客户端模块
"""
import os
import sys
import requests
from pathlib import Path

def load_env(env_path=None):
    """
    加载 .env 环境配置
    
    查找优先级：
    1. 显式 env_path 参数
    2. ARDC_PROJECT_ENV_FILE 环境变量
    3. <cwd>/.env.<ARDC_ENV> → <cwd>/config/.env.<ARDC_ENV>
    4. <cwd>/.env → <cwd>/config/.env
    """
    env_name = os.environ.get('ARDC_ENV', '')
    project_env_file = os.environ.get('ARDC_PROJECT_ENV_FILE', '')
    
    # 初始化环境变量
    env = {
        'ARDC_BASE_URL': os.environ.get('ARDC_BASE_URL', ''),
        'ARDC_API_KEY': os.environ.get('ARDC_API_KEY', ''),
        'ARDC_TOKEN': os.environ.get('ARDC_TOKEN', ''),
    }
    
    # 构建查找路径
    env_paths = []
    if env_path:
        env_paths.append(env_path)
    elif project_env_file:
        env_paths.append(project_env_file)
    else:
        if env_name:
            env_paths.append(f'.env.{env_name}')
            env_paths.append(f'config/.env.{env_name}')
        env_paths.append('.env')
        env_paths.append('config/.env')
    
    # 按优先级查找并加载
    for candidate in env_paths:
        if os.path.exists(candidate):
            with open(candidate) as f:
                for line in f:
                    line = line.strip()
                    if '=' in line and not line.startswith('#'):
                        k, v = line.split('=', 1)
                        env[k.strip()] = v.strip().strip('"').strip("'")
            print(f'[ardc_client] env loaded from {os.path.abspath(candidate)}', file=sys.stderr)
            break
    
    return env

def call_api(base_url, token, endpoint, body=None):
    """
    统一 API 调用接口
    
    Args:
        base_url: 服务地址
        token: API Token
        endpoint: API 端点
        body: 请求体
    
    Returns:
        JSON 响应
    """
    headers = {
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json',
    }
    
    try:
        resp = requests.post(
            f"{base_url}{endpoint}",
            json=body or {},
            headers=headers,
            timeout=(10, 60),
        )
        resp.raise_for_status()
        return resp.json()
    except requests.exceptions.RequestException as e:
        raise RuntimeError(f"API {endpoint} 调用失败：{e}")

def classify_image(base_url, token, image_path, model='efficientnet_b3'):
    """
    图片分类 API 调用
    
    Args:
        image_path: 图片路径
        model: 模型名称
    
    Returns:
        分类结果
    """
    with open(image_path, 'rb') as f:
        files = {'image': f}
        data = {'model': model}
        
        headers = {'Authorization': f'Bearer {token}'}
        
        resp = requests.post(
            f"{base_url}/api/classify",
            files=files,
            data=data,
            headers=headers,
        )
        
        return resp.json()
```

---

## 3. 第一个技能：ardc-collector

### 3.1 创建技能说明文件

**文件**: `skills/ardc-collector/SKILL.md`

```markdown
# ardc-collector - 数据采集技能

## 版本
1.0.0

## 功能说明

提供动漫/游戏角色图片的批量采集能力。

## 安装

```bash
ardc-skill-sync install ardc-collector
```

## 使用示例

### 批量采集图片

```bash
python ~/.ardc/skills/ardc-collector/scripts/collect_images.py \
    --role "Arona" \
    --count 100 \
    --output ./data/arona
```

### 爬取 URL

```bash
python ~/.ardc/skills/ardc-collector/scripts/crawl_urls.py \
    --keyword "blue archive arona" \
    --source "danbooru" \
    --output ./urls/arona_urls.txt
```

## 依赖技能

- ardc-client (必须)

## API 参考

| 脚本 | 功能 | 参数 |
|------|------|------|
| `collect_images.py` | 批量采集 | `--role`, `--count`, `--output` |
| `crawl_urls.py` | URL 爬取 | `--keyword`, `--source`, `--output` |
| `download_batch.py` | 批量下载 | `--url-file`, `--output`, `--concurrent` |
```

### 3.2 创建采集脚本

**文件**: `skills/ardc-collector/scripts/collect_images.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
批量采集角色图片
"""
import sys
import os
import argparse

# 添加 ardc-client 到路径
sys.path.insert(0, os.path.join(
    os.path.dirname(__file__),
    '..', '..', 'ardc-client', 'scripts'
))

from ard_client import load_env, call_api

def collect_images(role, count=100, output_dir='./data'):
    """
    批量采集角色图片
    
    Args:
        role: 角色名
        count: 目标数量
        output_dir: 输出目录
    """
    env = load_env()
    base_url = env.get('ARDC_BASE_URL', 'http://localhost:8080')
    token = env.get('ARDC_TOKEN', '')
    
    print(f"开始采集 {role} 的图片，目标数量：{count}")
    
    # 调用采集 API
    result = call_api(base_url, token, '/api/collect', {
        'role': role,
        'count': count,
        'output_dir': output_dir,
    })
    
    print(f"采集完成：{result}")
    return result

if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='批量采集角色图片')
    parser.add_argument('--role', required=True, help='角色名')
    parser.add_argument('--count', type=int, default=100, help='目标数量')
    parser.add_argument('--output', default='./data', help='输出目录')
    
    args = parser.parse_args()
    collect_images(args.role, args.count, args.output)
```

---

## 4. 技能管理工具

### 4.1 创建 ardc-skill-sync 工具

**文件**: `scripts/ardc-skill-sync.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
技能同步工具 - 安装/更新/管理技能包
"""
import argparse
import os
import sys
import json
import requests
from pathlib import Path

SKILL_HUB_URL = "https://skills.anime-role-detect.io"
SKILL_DIR = Path.home() / '.ardc' / 'skills'

def list_skills():
    """列出所有可用技能"""
    print("可用技能列表:")
    print("-" * 60)
    
    # 列出已安装技能
    if SKILL_DIR.exists():
        print("\n已安装技能:")
        for skill_path in SKILL_DIR.iterdir():
            if skill_path.is_dir():
                skill_file = skill_path / 'SKILL.md'
                if skill_file.exists():
                    # 简单解析版本号
                    version = "1.0.0"  # 默认版本
                    print(f"  ✓ {skill_path.name:20} v{version}")
    
    print("\n" + "-" * 60)
    print("\n提示：运行 'ardc-skill-sync install <skill>' 安装新技能")

def install_skill(skill_name, version=None):
    """安装指定技能"""
    print(f"正在安装 {skill_name}...")
    
    skill_dir = SKILL_DIR / skill_name
    skill_dir.mkdir(parents=True, exist_ok=True)
    
    # 模拟安装（实际应从远端下载）
    print(f"✓ 已安装 {skill_name} 到 {skill_dir}")
    print(f"提示：请手动将脚本复制到 {skill_dir}/scripts/")

def check_updates():
    """检查技能更新"""
    print("检查技能更新...")
    
    if not SKILL_DIR.exists():
        print("未安装任何技能")
        return
    
    for skill_path in SKILL_DIR.iterdir():
        if not skill_path.is_dir():
            continue
        
        skill_file = skill_path / 'SKILL.md'
        if skill_file.exists():
            print(f"  {skill_path.name}: 需要检查远端版本")

def main():
    parser = argparse.ArgumentParser(description='Anime Role Detect 技能管理工具')
    subparsers = parser.add_subparsers(dest='command', help='命令')
    
    # list 命令
    subparsers.add_parser('list', help='列出所有技能')
    
    # install 命令
    install_parser = subparsers.add_parser('install', help='安装技能')
    install_parser.add_argument('skill', help='技能名称')
    install_parser.add_argument('--version', help='指定版本')
    
    # check 命令
    subparsers.add_parser('check', help='检查更新')
    
    args = parser.parse_args()
    
    if args.command == 'list':
        list_skills()
    elif args.command == 'install':
        install_skill(args.skill, args.version)
    elif args.command == 'check':
        check_updates()
    else:
        parser.print_help()

if __name__ == '__main__':
    main()
```

### 4.2 使脚本可执行

```bash
chmod +x scripts/ardc-skill-sync.py
```

---

## 5. 测试技能

### 5.1 测试环境加载

```bash
cd /Users/caozhaoqi/PycharmProjects/anime_role_detect

# 测试加载环境
python3 -c "
import sys
sys.path.insert(0, 'skills/ardc-client/scripts')
from ardc_client import load_env
env = load_env()
print('环境配置:', env)
"
```

### 5.2 测试技能安装

```bash
# 列出技能
python3 scripts/ardc-skill-sync.py list

# 安装技能
python3 scripts/ardc-skill-sync.py install ardc-collector

# 检查更新
python3 scripts/ardc-skill-sync.py check
```

### 5.3 测试采集脚本

```bash
# 运行采集脚本（dry-run）
python3 skills/ardc-collector/scripts/collect_images.py \
    --role "Arona" \
    --count 10 \
    --output ./test_data/arona
```

---

## 6. 工作流编排

### 6.1 创建工作流文件

**文件**: `workflows/collect_and_train.yaml`

```yaml
name: 采集并训练工作流
description: 采集新角色数据并训练模型

steps:
  - name: 采集图片
    skill: ardc-collector
    script: collect_images.py
    args:
      role: "Arona"
      count: 200
      output: "./data/arona"
  
  - name: 数据清洗
    skill: ardc-cleaner
    script: deduplicate.py
    args:
      input: "./data/arona"
      output: "./data/arona_clean"
  
  - name: 训练模型
    skill: ardc-trainer
    script: train_model.py
    args:
      data-dir: "./data/arona_clean"
      model: "efficientnet_b3"
      epochs: 50
  
  - name: 模型评估
    skill: ardc-analyzer
    script: evaluate_model.py
    args:
      model-path: "./models/efficientnet_b3.pth"
      test-dir: "./data/arona_clean/val"
```

### 6.2 创建工作流执行器

**文件**: `scripts/run_workflow.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
工作流执行器
"""
import yaml
import subprocess
import argparse
from pathlib import Path

def run_workflow(workflow_file):
    """执行工作流"""
    with open(workflow_file) as f:
        workflow = yaml.safe_load(f)
    
    print(f"执行工作流：{workflow['name']}")
    print(f"描述：{workflow['description']}")
    print("-" * 60)
    
    for i, step in enumerate(workflow['steps'], 1):
        skill = step['skill']
        script = step['script']
        args = step.get('args', {})
        name = step.get('name', script)
        
        print(f"\n步骤 {i}/{len(workflow['steps'])}: {name}")
        
        # 构建命令
        cmd = [
            'python3',
            Path.home() / '.ardc' / 'skills' / skill / 'scripts' / script,
        ]
        
        for k, v in args.items():
            cmd.extend([f'--{k}', str(v)])
        
        # 执行
        try:
            subprocess.run(cmd, check=True)
            print(f"✓ 步骤 {i} 完成")
        except subprocess.CalledProcessError as e:
            print(f"✗ 步骤 {i} 失败：{e}")
            break
    
    print("\n" + "=" * 60)
    print("工作流执行完成")

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--workflow', required=True, help='工作流 YAML 文件')
    args = parser.parse_args()
    
    run_workflow(args.workflow)
```

---

## 7. 下一步

### 7.1 迁移现有脚本

将现有脚本按功能分类迁移到对应技能目录：

```bash
# 数据采集类
cp scripts/data_collection/*.py skills/ardc-collector/scripts/

# 数据清洗类
cp scripts/data_cleaning/*.py skills/ardc-cleaner/scripts/

# 模型训练类
cp scripts/model_training/*.py skills/ardc-trainer/scripts/

# 分类推理类
cp scripts/classification/*.py skills/ardc-classifier/scripts/

# 分析类
cp scripts/analysis/*.py skills/ardc-analyzer/scripts/
```

### 7.2 编写技能文档

为每个技能编写详细的 `SKILL.md` 文档，包括：
- 功能说明
- 安装方法
- 使用示例
- API 参考
- 依赖关系

### 7.3 测试和优化

- 测试各技能独立运行
- 测试工作流编排
- 优化性能和错误处理

---

## 8. 常见问题

### Q: 如何切换环境？

```bash
# 使用开发环境
export ARDC_ENV=dev
python3 scripts/ardc-skill-sync.py list

# 使用生产环境
export ARDC_ENV=prod
python3 scripts/ardc-skill-sync.py list
```

### Q: 如何自定义配置路径？

```bash
export ARDC_PROJECT_ENV_FILE=/path/to/your/.env
python3 scripts/ardc-skill-sync.py install ardc-collector
```

### Q: 如何调试技能？

```bash
# 启用详细日志
export ARDC_LOG_LEVEL=DEBUG
python3 skills/ardc-collector/scripts/collect_images.py --role "Arona" --count 10
```

---

**文档版本**: 1.0  
**生成日期**: 2026 年 5 月 18 日  
**适用项目**: Anime Role Detect