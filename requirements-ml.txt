# ==============================================
# 机器学习专属依赖 - 仅包含 requirements.txt 中没有的 ML 包
# PyTorch 生态不锁死版本，由 pip 自动解析兼容组合
# ==============================================

# 深度学习框架（CPU 版，让 pip 解析兼容版本组合）
torch
torchvision
torchaudio

# numpy（opencv-python-headless 需要 numpy>=2）
numpy>=2.0.0

# 向量嵌入
sentence-transformers==3.0.1

# 训练加速
accelerate==0.34.2

# 实验追踪
mlflow==2.16.2
