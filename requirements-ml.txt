# ==============================================
# 机器学习专属依赖 - 仅包含 requirements.txt 中没有的 ML 包
# PyTorch 在 Dockerfile.ml-base 中单独安装（CPU 版本）
# numpy==1.26.4 兼容 Windows + Python 3.14
# ==============================================

# numpy（兼容 Windows + Python 3.14，与 opencv-python-headless 兼容）
numpy==1.26.4

# 向量嵌入
sentence-transformers==3.0.1

# 训练加速
accelerate==0.34.2

# 实验追踪
mlflow==2.16.2

# CLIP 模型（用于搜索和去重）
git+https://github.com/openai/CLIP.git

# 向量索引（用于搜索）
faiss-cpu==1.8.0