ARG REGISTRY_MIRROR=docker.io
ARG BASE_IMAGE=ard-base
FROM ${BASE_IMAGE}

COPY .env.example .env
COPY src/ ./src/

RUN mkdir -p logs run

EXPOSE 8000

CMD ["python", "src/services/api_gateway/app.py"]
