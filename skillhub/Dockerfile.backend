FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ardc/ ardc/
COPY setup.py .
RUN pip install -e .

EXPOSE 8000

CMD ["uvicorn", "ardc.api.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]