# ==============================================
# 基础依赖 - Base Dependencies
# 用于生产环境的核心依赖
# python-jose/passlib 已移至 requirements-scripts.txt（仅skillhub使用）
# ==============================================

# Web框架
fastapi==0.115.0
uvicorn==0.39.0
flask==3.1.3

# 数据验证
pydantic==2.13.4
pydantic-settings==2.5.2

# HTTP客户端
requests==2.32.5
aiohttp==3.13.5
aio_pika==9.5.6
pika==1.4.1

# 文件处理
python-multipart==0.0.20

# 监控指标
prometheus_client==0.25.0

# 数据库
sqlalchemy==2.0.50
redis==7.0.1

# 配置管理
python-dotenv==1.0.1

# 日志
loguru==0.7.3

# HTTP客户端
httpx==0.28.1

# 图像处理
Pillow==11.3.0
scikit-image==0.22.0

# 数值计算
numpy>=1.26.4

# 关键点检测
mediapipe>=0.10.35

# 系统监控
psutil==7.2.2

# OpenTelemetry（链路追踪）- 版本需保持一致
# API/SDK 1.x.x 对应 instrumentation/semantic-conventions 0.(x+21).x
opentelemetry-api==1.41.1
opentelemetry-sdk==1.41.1
opentelemetry-semantic-conventions==0.62b1
opentelemetry-instrumentation-fastapi==0.62b1
opentelemetry-exporter-otlp-proto-grpc==1.41.1
opentelemetry-exporter-otlp-proto-common==1.41.1
opentelemetry-proto==1.41.1

# ==============================================
# 运行时依赖（base 镜像服务需要，非 ML）
# CI / K8s 中 celery-worker、celery-beat 复用 ardc/api-service 镜像并以
# `celery -A src.core.celery_config ...` 启动，必须安装 celery。
# faiss / pymysql / bcrypt / PyJWT / cryptography / scikit-learn 为
# 搜索、数据库、认证等模块运行时所需（本地 .venv 有完整 requirements.txt，
# 但 ardc/base 仅装 requirements-base.txt，缺这些会导致容器启动失败）。
# ==============================================
celery==5.4.0
faiss-cpu==1.9.0
pymysql==1.1.1
bcrypt==4.2.0
PyJWT==2.13.0
cryptography==48.0.1
scikit-learn==1.6.1

# ONNX 推理 + 进度条：celery-worker/celery-beat 复用 ardc/api-service 镜像，
# src/tasks/image_tasks.py 顶层 `from src.core.onnx_engine import get_engine`
# → onnx_engine.py 顶层 `import onnxruntime as ort`。缺 onnxruntime 会让 celery
# 在 import 阶段直接 ModuleNotFoundError 崩溃 (CrashLoopBackOff)。
# api-service 的 onnx_inference 路由、cleaning_routes 也分别需要 onnxruntime / tqdm。
onnxruntime==1.19.2
tqdm==4.67.1
