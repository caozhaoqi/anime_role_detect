模型优化（进一步压榨性能）
模型量化 (Quantization)：目前是标准 PyTorch 格式。建议尝试导出为 CoreML 格式并开启 INT8 或 FP16 量化。在 Apple Silicon 上，CoreML 结合 ANE（神经网络引擎）可能会将速度进一步提升至 150+ FPS，同时大幅降低功耗。
知识蒸馏：尝试以该 B3 模型为“教师”，训练一个 EfficientNet-B0 或 MobileNet-V3 的“学生”模型。如果能保持 90% 以上的准确率，将更适合大规模并发或移动端实时检测。
2. 业务功能扩展
Open-set 识别：利用你现有的 FAISS 体系，建立一个“未知角色”阈值。当模型提取的特征与库中所有角色的相似度都低于某个值时，判定为“未知”，而不是强行匹配给 74 个角色之一。
多目标检测集成：目前是分类模型。下一步可以结合 YOLOv8/v10 做“人脸/人体框选 + 角色识别”，实现一张图里同时识别多个角色。
3. 工程化建议
自动化反馈闭环：在应用场景中，记录那些置信度在 0.5-0.7 之间的“模糊样本”，人工标注后加入 data/final_dataset 进行增量训练。
版本控制：当前版本是 v2。建议保留 training_results.json 中的超参数记录，以便未来对比 v3（如果引入了数据增强如 CutMix/MixUp）的效果。